---
interface Props {
  as?: "blockquote" | "p";
  align?: "start" | "center";
  class?: string;
}
const { as: Tag = "blockquote", align = "start", class: className = "" } = Astro.props;
---
<Tag class:list={["ds-quote", `ds-text--${align}`, className]}><slot /></Tag>
