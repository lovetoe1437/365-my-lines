---
interface Props {
  as?: "p" | "span" | "div";
  align?: "start" | "center" | "end";
  class?: string;
}
const { as: Tag = "p", align = "start", class: className = "" } = Astro.props;
---
<Tag class:list={["ds-eyebrow", `ds-text--${align}`, className]}><slot /></Tag>
